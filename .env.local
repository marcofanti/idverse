# IDVerse API Configuration
# Replace these values with your actual IDVerse API credentials
# This file serves as a template for setting up your local environment

# OAuth credentials for authenticating with the IDVerse API
# Obtain these from your IDVerse account dashboard
IDVERSE_CLIENT_ID=your_client_id_here
IDVERSE_CLIENT_SECRET=your_client_secret_here

# IDVerse API endpoints
# Update the domain to match your IDVerse environment (e.g., us.demo, eu.prod)
IDVERSE_OAUTH_URL=https://domain.idkit.co/api/3.5/oauthToken
IDVERSE_API_URL=https://domain.idkit.co/api/3.5/sendSms

# Logging verbosity level
# Options: DEBUG, INFO, SECRET (logs complete request with credentials)
# WARNING: SECRET mode will log sensitive credentials - use only in development
VERBOSE=DEBUG

# Default form field values (optional)
# These values will pre-populate the web form for faster testing

# Transaction ID - default value for the transaction ID field
TRANSACTION=tmx-dkjhas9843

# Name - default value for the name field
NAME=Marco

# Supplied First Name - default value for the supplied first name field
SUPPLIED_FIRST_NAME=Marco

# Webhook Configuration
# URLs for receiving webhook notifications from IDVerse API

# Completion webhook URL - receives final transaction status notifications
# Events: completedPass, completedFlagged, expired, cancelled
NOTIFY_URL_COMPLETE=http://localhost:8080/api/webhook

# Event webhook URL - receives all transaction stage notifications
# Events: pending, termsAndConditions, idSelection, personalDetails, liveness,
#         expired, cancelled, completedPass, completedFlagged
NOTIFY_URL_EVENT=http://localhost:8080/api/webhook

# JWT Secret Key for webhook authentication
# IMPORTANT: Change this to a strong, random secret in production
# Minimum 32 characters recommended for HS256 algorithm
JWT_SECRET_KEY=your-secret-key-minimum-32-chars-change-in-production
